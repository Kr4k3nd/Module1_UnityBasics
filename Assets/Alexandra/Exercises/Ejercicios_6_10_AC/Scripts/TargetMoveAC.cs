using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Jobs;
using static UnityEngine.GraphicsBuffer;
using static UnityEngine.ParticleSystem;

public class TargetMoveAC : MonoBehaviour
{
    /*
    public Rigidbody rb;
    public GameObject TargetAC;     //mover el objeto indicado cubo pa los amigos
    

    /
    // Start is called before the first frame update
    void Start()
    {
        


    }

    // Update is called once per frame
    void Update()
    {
        // Punto de inicio del cubo

        transform.position = new Vector3(-0.49f, -0.21f, -8.98f);

       
        private void OnTriggerEnter(Collider other)
        {
            //Ejercicios 2 y 3 La bola se destruye y hace deja un sistema de particulas detras
            //Instantiate(Pop, transform.position, Quaternion.identity);
            Destroy(gameObject);

            //En cuanto entre en contacto que pete y se destruya
            Debug.Log("Entra");

        }
        private void OnTriggerEnter(Collider other)     // hacemos que cuando entre en Trigger por colisión haga lo siguiente:
        {
            particles.Play();
            Destroy(other.gameObject);              // destruye el objeto del juego
*/
        }
    