using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instantiate_SR : MonoBehaviour
{
    /*
    public GameObject prefabToInstantiate;                      //objeto a instanciar
    public Transform waypoint;                                  //el sitio donde los instanciaremos        

    public float time;                                          // tiempo en el que tardara en instanciarse
    private float resetTime;                                    // el valor en el que va a resetear el tiempo

    // Start is called before the first frame update
    void Start()
    {
        resetTime = time;
    }

    // Update is called once per frame
    void Update()
    {
        time = time -1 * Time.deltaTime;

        if (time < 0)
        {
            Instantiate_SR(prefabToInstantiate, waypoint);
            time = resetTime;
        }
    }
    */
}

